iven a string s of lower and upper case English letters.

A good string is a string which doesn't have two adjacent characters s[i] and s[i + 1] where:

0 <= i <= s.length - 2
s[i] is a lower-case letter and s[i + 1] is the same letter but in upper-case or vice-versa.
To make the string good, you can choose two adjacent characters that make the string bad and remove them. You can keep doing this until the string becomes good.

Return the string after making it good. The answer is guaranteed to be unique under the given constraints.

Notice that an empty string is also good.

 

Example 1:

Input: s = "leEeetcode"
Output: "leetcode"
Explanation: In the first step, either you choose i = 1 or i = 2, both will result "leEeetcode" to be reduced to "leetcode".
Example 2:

Input: s = "abBAcC"
Output: ""
Explanation: We have many possible scenarios, and all lead to the same answer. For example:
"abBAcC" --> "aAcC" --> "cC" --> ""
"abBAcC" --> "abBA" --> "aA" --> ""
Example 3:

Input: s = "s"
Output: "s"

"Pp" -> ""

lập 2 vòng lập
    vongf số lần lập
        vòng 2 kiểm tra s[i] = s[i+1] + 32 hoặc s[i] + 32 = s[i+1]
            đúng thì xóa bỏ vị trí i với 2 đơn vị ra
    return chỗi đã chỉnh sửa

class Solution {
public:
    string makeGood(string s) {
        string result = s;
        for(int k = 0 ; k < s.size() -1 ; k++){
            for( int i = 0; i < result.size(); i++){
                if(result[i] == (result[i+1] + 32) || (result[i] + 32) == result[i+1]) {
                    result.erase(i,2);
                    break;
                }
            }
        }
        return result;
    }
};

tạo mãng res
duyệt mãng s
    thêm từng phần từ vào res 
    if kiểm tra res.size > 0 và res.back() == s[i+1] + 32 || res.back() + 32 = s[i+1] 
        thì xóa res.pop_back
        i++; - i nhảy 2 đơn vị

return res

dung stack 
    stack 
    duyet mang tu 0 den cuoi mang
    if stack không rỗng thì kiểm tra Math.abs (stack.peek()-s.charAt(i)) == 32 ko
    đúng thì xóa stack vừa thêm ra
    sai : thêm phần từ đó vào stack

    tạo ra mãng res có size bằng stack 
    nạp phần từ bằng đi từ cuối lên 

    return new string (res);


class Solution {
    public String makeGood(String s) {
        Stack<Character>  ans =  new Stack() ;
        for(int i = 0; i < s.length(); i++){
            
            if(!ans.isEmpty() && Math.abs(ans.peek() - s.charAt(i)) == 32)
            {
                ans.pop();
                
            } else{
                ans.push(s.charAt(i));
            }
            
        }
        char res[] = new char[ans.size()];
        int index = ans.size()-1;
        while(!ans.isEmpty()){
            res[index--] = ans.pop();
        }
        return new String (res);
    }
}