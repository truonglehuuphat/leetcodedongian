Given the head of a singly linked list where elements are sorted in ascending order, convert it to a height-balanced binary search tree.

Input: head = [-10,-3,0,5,9]
Output: [0,-3,9,-10,null,5]
Explanation: One possible answer is [0,-3,9,-10,null,5], which represents the shown height balanced BST.
Example 2:

Input: head = []
Output: []
 

Constraints:

The number of nodes in head is in the range [0, 2 * 104].
-105 <= Node.val <= 105



/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    TreeNode myTree;
    TreeNode *insertBST(TreeNode *root, int value){
        if(root == NULL){
            return new TreeNode(value);
        }
        if(value < root->val){
            if(root->left == NULL){
                return new TreeNode(value);
            } 
            else{
              root->left = insertBST(root->left, value);
            }
        } else {
            if(root->right == NULL){
                return new TreeNode(value);
            } 
            else{
              root->right = insertBST(root->right, value);
            } 
        }
        return root;
    }
    TreeNode* insert(TreeNode *tree, int value){
        if(tree == NULL) {
            tree = new TreeNode(value);
            return tree;
        } else{
            while(1){
                TreeNode* temp = tree;
                if(value > temp->val){
                    if(temp->right == NULL){
                        temp->right = new TreeNode(value);
                        cout<< " 1 ";
                        break;
                    } else {
                        temp = temp->right;
                        cout<< " 2 ";
                    }
                } else {
                    if(temp->left == NULL){
                        temp->left = new TreeNode(value);
                        cout<< " 3 ";
                        break;
                    } else {
                        temp = temp->left;
                        cout<< " 4 ";
                    }                
                }
            }
            return tree;
        }
    }
    void PreOrder(TreeNode * root){
        if(root != NULL){
            cout << root->data;
            PreOrder(root->left);
            PreOrder(root->right);
        }
    }

    TreeNode* sortedListToBST(ListNode* head) {
        if(head == NULL) return NULL;
        vector<int> arr; 
        while(head){
            arr.push_back(head->val);
            head = head->next;
        }
        if(arr.size() > 0){
            int mildePosition = arr.size()/ 2;
            int valueMidPosition = arr[mildePosition];
            TreeNode *rootTree = new TreeNode(valueMidPosition, NULL, NULL);
            for(int i = arr.size()/ 2 - 1; i >= 0; i--){
                rootTree = insertBST(rootTree, arr[i]);
            }
            for(int i = arr.size()/ 2 + 1; i < arr.size(); i++){
                rootTree = insertBST(rootTree, arr[i]);
            }
            vector<int> arr;
            return rootTree;
        }
        return NULL;
    }
};


/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    TreeNode myTree;
    TreeNode *insertBST(TreeNode *root, int value){
        if(root == NULL){
            return new TreeNode(value);
        }
        if(value < root->val){
            root->left = insertBST(root->left, value);
        } else {
            root->right = insertBST(root->right, value);
        }
        return root;
    }
    void PreOrderTreeNode(TreeNode * root){
        if(root != NULL){
            cout << root->val << " ";
            PreOrderTreeNode(root->left);
            PreOrderTreeNode(root->right);
        }
    }
    void vectorInput(vector<int> arr){
        for(int i = 0 ; i < arr.size(); i++ ){
             cout << arr[i]<< " ";
        }
           cout << "\n";
    }

    TreeNode* sortedListToBST(ListNode* head) {
        if(head == NULL) return NULL;
        vector<int> arr; 
        while(head){
            arr.push_back(head->val);
            head = head->next;
        }
        if(arr.size() > 0){
            // vectorInput(arr);
            int mildePosition = arr.size()/ 2;
            int valueMidPosition = arr[mildePosition];
            TreeNode *rootTree = new TreeNode(valueMidPosition, NULL, NULL);
            // PreOrderTreeNode(rootTree);
            // cout << "\n";
            for(int i = arr.size() - 1; i >= 0; i--){
                if(i == mildePosition) continue;
                rootTree = insertBST(rootTree, arr[i]);
            }
            // PreOrderTreeNode(rootTree);
            // for(int i = mildePosition + 1; i < arr.size(); i++){
            //     rootTree = insertBST(rootTree, arr[i]);
            // }
            PreOrderTreeNode(rootTree);
            return rootTree;
        }
        return NULL;
    }
};


/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     int height;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    TreeNode myTree;
// struct TreeNode {
//       int val;
//       TreeNode *left;
//       TreeNode *right;
//       int height;
//       TreeNode() : val(0), left(nullptr), right(nullptr) {}
//       TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
//       TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
//   };
    int max(int a, int b){
        return a > b ? a : b;
    }
    // int height (TreeNode * N){
    //     if( N == NULL) return 0;
    //     return N->height;
    // }
    // TreeNode *rightRotate(TreeNode *y){
    //     TreeNode *x = y->left;
    //     TreeNode *T2 = x->right;
    //     x->right = y;
    //     y->left = T2;
    //     y->height = max(height(y->left) , height(y->right)) +1;
    //     x->height = max(height(y->left) , height(y->right)) +1;
    //     return x;
    // }
    // TreeNode *leftRotate(TreeNode *x){
    //     TreeNode *y = x->right;
    //     TreeNode *T2 = y->left;
    //     y->left = x;
    //     x->right = T2;
    //     y->height = max(height(y->left) , height(y->right)) +1;
    //     x->height = max(height(y->left) , height(y->right)) +1;
    //     return y;
    // }
    // int getBalance(TreeNode *N){
    //     if (N == NULL)
    //         return 0;
    //     return height(N->left) - height(N->right);
    // }    

    TreeNode *insertBST(TreeNode *root, int value){
        if(root == NULL){
            return new TreeNode(value);
        }
        if(value < root->val){
            root->left = insertBST(root->left, value);
        } else if (value > root->val) {
            root->right = insertBST(root->right, value);
        } else {

        }
        // root->height = 1 + max(height(root->left), height(root->right));
        // int balance = getBalance(root);
        // if (balance > 1 && value < root->left->val)
        //     return rightRotate(root);
        // if (balance < -1 && value > root->right->val)
        //     return leftRotate(root);
        // if (balance > 1 && value > root->left->val) {
        //     root->left = leftRotate(root->left);
        //     return rightRotate(root);
        // }
        // if (balance < -1 && value < root->right->val) {
        //     root->right = rightRotate(root->right);
        //     return leftRotate(root);
        // }
        return root;
    }
    void PreOrderTreeNode(TreeNode * root){
        if(root != NULL){
            cout << root->val << " ";
            if(root->left == NULL) cout << "L-NULL ";
            if(root->right == NULL) cout << "R-NULL ";
            PreOrderTreeNode(root->left);
            PreOrderTreeNode(root->right);
        }
    }
    void vectorInput(vector<int> arr){
        for(int i = 0 ; i < arr.size(); i++ ){
             cout << arr[i]<< " ";
        }
           cout << "\n";
    }

    TreeNode* sortedListToBST(ListNode* head) {
        if(head == NULL) return NULL;
        vector<int> arr; 
        // TreeNode *rootTree = NULL;
        while(head){
            arr.push_back(head->val);
            // rootTree = insertBST(rootTree, head->val);
            head = head->next; 
        }
        if(arr.size() > 0){
        //     // vectorInput(arr);
            int mildePosition = 0;
            mildePosition = (arr.size()/ 2);
            int valueMidPosition = arr[mildePosition];
            TreeNode *rootTree = new TreeNode(valueMidPosition, NULL, NULL);
            rootTree = insertBST(rootTree, arr[mildePosition - mildePosition/2]);
            rootTree = insertBST(rootTree, arr[mildePosition + mildePosition/2]);

            for(int i = mildePosition+1; i < arr.size(); i++){
                    if( i == (mildePosition + mildePosition/2)) continue;
                    rootTree = insertBST(rootTree, arr[i]);
            }
            for(int i = mildePosition-1; i >= 0; i--){
                    if(i == (mildePosition - mildePosition/2)) continue;
                    rootTree = insertBST(rootTree, arr[i]);
            }            
            // for(int i = 0; i < arr.size(); i=i+mildePosition){
            //     if(i == mildePosition) continue;
            //     rootTree = insertBST(rootTree, arr[i]);
            // }            
            // PreOrderTreeNode(rootTree);
            // cout << "\n";
            // if(valueMidPosition % 2 != 0){
            //     for(int i = 1; i < arr.size(); i=i+2){
            //         if(i == mildePosition) continue;
            //         rootTree = insertBST(rootTree, arr[i]);
            //     }
            //     for(int i = 0; i < arr.size(); i=i+2){
            //         if(i == mildePosition) continue;
            //         rootTree = insertBST(rootTree, arr[i]);
            //     }
            // } else{
            //     for(int i = mildePosition; i < arr.size(); i=i+2){
            //         if(i == mildePosition) continue;
            //         rootTree = insertBST(rootTree, arr[i]);
            //     }
            //     for(int i = mildePosition; i >= 0; i=i-2){
            //         if(i == mildePosition) continue;
            //         rootTree = insertBST(rootTree, arr[i]);
            //     }                
            //     for(int i = 1; i < arr.size(); i=i+2){
            //         if(i == mildePosition) continue;
            //         rootTree = insertBST(rootTree, arr[i]);
            //     }                
            // }

            PreOrderTreeNode(rootTree);
            return rootTree;
        }

        return NULL;
    }
};